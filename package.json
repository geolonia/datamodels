{
  "name": "geonicdb-models",
  "private": true,
  "version": "0.0.0",
  "description": "Curated bilingual catalog of NGSI-LD data models for GeonicDB, served at models.geonicdb.com.",
  "type": "module",
  "license": "Apache-2.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "check:immutability": "node scripts/check-immutability.mjs",
    "manifest:record": "npm run validate:models && npm run build && node scripts/check-immutability.mjs --record",
    "check": "npm run build:deploy && wrangler deploy --dry-run --outdir .wrangler/dry-run",
    "dev": "npm run build && wrangler dev",
    "deploy": "npm run build:deploy && wrangler deploy",
    "validate:models": "node scripts/validate-models.mjs",
    "test": "node --test \"test/**/*.test.mjs\"",
    "export:geonicdb": "node scripts/export-geonicdb.mjs",
    "build:deploy": "npm run validate:models && npm run build && npm run check:immutability"
  },
  "devDependencies": {
    "ajv": "^8.20.0",
    "ajv-formats": "^3.0.1",
    "jsonld": "^8.3.3",
    "wrangler": "4.127.0",
    "yaml": "^2.9.1"
  }
}
