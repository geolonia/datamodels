{
  "name": "geonicdb-models",
  "private": true,
  "version": "0.0.0",
  "description": "Curated bilingual catalog of NGSI-LD data models for GeonicDB, served at models.geonicdb.com.",
  "type": "module",
  "license": "Apache-2.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "check:immutability": "node scripts/check-immutability.mjs",
    "manifest:record": "npm run build && node scripts/check-immutability.mjs --record",
    "check": "npm run build && npm run check:immutability && wrangler deploy --dry-run --outdir .wrangler/dry-run",
    "dev": "npm run build && wrangler dev",
    "deploy": "npm run build && npm run check:immutability && wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "4.127.0"
  }
}
