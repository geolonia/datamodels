apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  namespace: geolonia
  name: datamodels
  description: Bilingual catalog of data models that work in Japan (JSON Schemas, JSON-LD contexts, RDFS vocabularies; ready for NGSI-LD), served at datamodels.jp. Product-neutral; extends Smart Data Models, GIF and RFC 8984.
  annotations:
    github.com/project-slug: geolonia/datamodels
  tags: ['website', 'data-models', 'json-schema', 'json-ld', 'linked-data', 'ngsi-ld', 'fiware']
  links:
    - url: https://datamodels.jp
      title: Data models catalog (public site)
      icon: web
    - url: https://github.com/geolonia/datamodels/blob/main/docs/design.md
      title: Design document
      icon: docs
spec:
  type: 'website'
  lifecycle: 'experimental'
  owner: group:geolonia/geonicdb
  # accessGrants:
  #   - team: group:geolonia/team-name
  #     permission: write  # read | triage | write | maintain | admin
