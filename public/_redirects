# Redirects for models.geonicdb.com (Workers static assets).
#
# Type and attribute IRIs under /ns/ resolve to the model page that defines
# them, schema.org style. Entries are added with each model, for example:
#   /ns/jp/Shelter  /models/jp/Shelter/  302
