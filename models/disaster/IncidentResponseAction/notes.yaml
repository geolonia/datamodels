notes:
  - Seeded 2026-09-17 from geolonia/geonicdb-datamodels data-models/*.json (Takamatsu City flood-prevention headquarters). Type name generalised from the tenant-specific "Saitai" prefix.
  - "2.0.0: a subclass of task Task. task → parent, content → name, isDone → progress, assignee is now a multi-valued Relationship to team or person entities (was a free-text team name), updatedAt → dateModified; project added because Task requires it."
  - "2.1.0: project is optional again, following task 1.1.0; the parent report carries the disaster event."
license: CC BY 4.0 (see /LICENSE-CONTENT.md)
